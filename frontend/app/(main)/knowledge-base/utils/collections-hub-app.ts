/**
 * Previously contained helpers for the synthetic `knowledgeBase_<orgId>` hub app.
 * In the new model each KB is its own root-level `apps` document; there is no shared
 * hub app. These exports are kept empty to avoid breaking any remaining import sites
 * while the broader codebase is updated.
 *
 * @deprecated All functions in this file are obsolete and should be removed once all
 * callers have been updated to use the navigation API directly.
 */
