name: Integration Tests

on:
  pull_request_target:
    types: [opened, synchronize, reopened, labeled, unlabeled]
    branches:
      - 'main'
  # Daily at 00:00 IST (Asia/Kolkata); Actions cron is UTC → 18:30 UTC.
  schedule:
    - cron: '30 18 * * *'
  workflow_dispatch:
    inputs:
      connectors:
        description: 'Connector markers to run (e.g. "s3", "gcs", "azure_blob", "azure_files", or "integration" for all)'
        required: true
        default: 'integration'
        type: choice
        options:
            - integration
            - s3
            - postgres
            - mariadb
            - nextcloud
            - bookstack
            - minio
            - sharepoint
            - confluence
            - jira
            - linear
            - github_teams
            - github
            - gitlab
            - notion
            - web
            - rss
            - local_fs
            - google_drive_workspace
            - google_drive_individual
            - gcs
            - azure_blob
            - azure_files
            - messaging
            - oauth_clients
            - mcp
            - zammad
            - demo
            - resilience
            - permissions
            - storage
      commit_hash:
        description: 'Commit hash to checkout (defaults to HEAD)'
        required: false
        default: ''
      python_version:
        description: 'Python version to use'
        required: true
        default: '3.12'
        type: choice
        options:
          - '3.10'
          - '3.11'
          - '3.12'
      graph_db:
        description: 'Graph database backend to test against'
        required: true
        default: 'both'
        type: choice
        options:
          - arango
          - neo4j
          - both
      run_mode:
        description: 'When graph_db is "both": run the two backends sequentially in one job (default) or as parallel jobs on separate runners'
        required: true
        default: 'sequential'
        type: choice
        options:
          - sequential
          - parallel
      message_broker:
        description: 'Message broker to use'
        required: true
        default: 'redis'
        type: choice
        options:
          - kafka
          - redis
      pytest_workers:
        description: 'pytest-xdist worker count. Connector suites are grouped onto a single worker to keep their ordering; enterprise-search tests spread across all workers. 1 disables parallelism. Each worker is a separate interpreter costing ~1 GB, and the runner has ~15.7 GB shared with the whole Docker stack — raising this risks OOM-killing the runner.'
        required: true
        default: '2'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'
          - '6'
          - '7'
          - '8'

permissions:
  contents: read

# The matrix reads labels from each event's frozen payload, so an opened/synchronize
# event firing before a label lands resolves to the neo4j default and would otherwise
# burn a runner alongside the labeled run. Non-PR events get a unique group per run so
# a manual dispatch never cancels the nightly cron.
concurrency:
  group: integration-tests-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

env:
  PIPESHUB_BASE_URL: http://localhost:3000
  PYTEST_WORKERS: ${{ github.event.inputs.pytest_workers || '2' }}

jobs:
  # Which parts the run tests on besides the graph databases (always both).
  # Nightly runs rotate through the supported choices by weekday; pull requests
  # use the default; a manual run uses what it asked for (scripts/nightly_setup.py).
  setup:
    name: Pick the setup to test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      message_broker: ${{ steps.pick.outputs.message_broker }}
      label: ${{ steps.pick.outputs.label }}
      is_default: ${{ steps.pick.outputs.is_default }}
    steps:
      # The base branch's copy on pull_request_target, never the PR's.
      - name: Checkout the picker
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          sparse-checkout: scripts
      - name: Pick the setup
        id: pick
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_BROKER: ${{ github.event.inputs.message_broker }}
        run: python3 scripts/nightly_setup.py --event "$EVENT_NAME" --message-broker "$REQUESTED_BROKER"

  integration-tests:
    name: Integration Tests (${{ matrix.graph_db }} · ${{ matrix.shard }}${{ needs.setup.outputs.is_default == 'false' && format(' · {0}', needs.setup.outputs.message_broker) || '' }})
    needs: setup
    if: ${{ github.event_name != 'pull_request_target' || !contains(github.event.pull_request.labels.*.name, 'skip-it') }}
    runs-on: runs-on=${{ github.run_id }}/runner=4cpu-linux-x64/volume=100gb/ssh=true
    timeout-minutes: 300

    strategy:
      fail-fast: false
      matrix:
        # One job per value; "both" runs both backends sequentially in one job.
        # PRs default to neo4j alone; it-arango / it-both / it-parallel (or
        # it-neo4j + it-arango) widen that. Cron and dispatch still get "both".
        # `strategy` can't read the job env block, so derive from inputs/labels here.
        graph_db: ${{ fromJSON((github.event.inputs.graph_db == 'arango' && '["arango"]') || (github.event.inputs.graph_db == 'neo4j' && '["neo4j"]') || (contains(github.event.pull_request.labels.*.name, 'it-neo4j') && contains(github.event.pull_request.labels.*.name, 'it-arango') && '["arango","neo4j"]') || (contains(github.event.pull_request.labels.*.name, 'it-neo4j') && '["neo4j"]') || (contains(github.event.pull_request.labels.*.name, 'it-arango') && '["arango"]') || ((github.event.inputs.run_mode == 'parallel' || contains(github.event.pull_request.labels.*.name, 'it-parallel')) && '["arango","neo4j"]') || (contains(github.event.pull_request.labels.*.name, 'it-both') && '["both"]') || (github.event_name == 'pull_request_target' && '["neo4j"]') || '["both"]') }}
        # Split the suite into parallel shards, each with its own stack, so the
        # slow connector suites do not all serialize on one worker. Three
        # connector shards (balanced on measured minutes, see
        # scripts/shard_durations.json) plus a "core" shard for everything else.
        # Each shard's marker is derived in the test step from CONN_SHARD_1 /
        # CONN_SHARD_2 / CONN_SHARD_3 (one source of truth); "core" is everything
        # those three do not own, so the four partition -m "integration" exactly.
        # scripts/shard_balance.py checks that partition and the balance; run it
        # after adding or growing a connector suite.
        # A workflow_dispatch of one marker (a single connector or suite rather
        # than the whole of `integration`) runs as a single "dispatch" job that
        # selects exactly that marker, so it runs once rather than on every shard.
        shard: ${{ fromJSON((github.event_name == 'workflow_dispatch' && github.event.inputs.connectors != '' && github.event.inputs.connectors != 'integration') && '["dispatch"]' || '["connectors-1","connectors-2","connectors-3","core"]') }}

    # One shared env; its required reviewers are the approval gate. Per-backend
    # approval = run only that backend (it-arango/it-neo4j), not a per-backend env.
    # Schedule uses integration-test-daily to bypass deployment protection.
    environment:
      name: ${{ github.event_name == 'schedule' && 'integration-test-daily' || 'integration-test-dev' }}

    env:
      MESSAGE_BROKER: ${{ needs.setup.outputs.message_broker }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      DOCKER_BUILDKIT: "1"
      PIPESHUB_TEST_ENV: local
      # A connector suite whose credential is missing skips itself, which on the
      # nightly is indistinguishable from a pass. On the runs that were meant to
      # cover these connectors — the nightly, and a dispatch that picked one —
      # a missing credential is a failure instead.
      PIPESHUB_REQUIRE_CONNECTOR_SECRETS: ${{ (github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.event.inputs.connectors != 'integration')) && '1' || '' }}
      ARANGO_PASSWORD: ${{ secrets.TEST_ARANGO_PASSWORD }}
      PIPESHUB_TEST_USER_EMAIL: ${{ secrets.PIPESHUB_TEST_USER_EMAIL }}
      PIPESHUB_TEST_USER_PASSWORD: ${{ secrets.PIPESHUB_TEST_USER_PASSWORD }}
      TEST_NEO4J_URI: ${{ secrets.TEST_NEO4J_URI }}
      TEST_NEO4J_USERNAME: ${{ secrets.TEST_NEO4J_USERNAME }}
      TEST_NEO4J_PASSWORD: ${{ secrets.TEST_NEO4J_PASSWORD }}
      TEST_NEO4J_DATABASE: ${{ secrets.TEST_NEO4J_DATABASE }}
      S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
      # The PostgreSQL connector syncs from a database that runs in the
      # integration stack, so these are fixed test values rather than
      # secrets. The connector reaches it by service name from inside the
      # compose network; the test process uses the published port.
      POSTGRES_TEST_HOST: localhost
      POSTGRES_TEST_PORT: '5433'
      POSTGRES_CONNECTOR_HOST: postgres-source
      POSTGRES_CONNECTOR_PORT: '5432'
      POSTGRES_TEST_USER: pipeshubtest
      POSTGRES_TEST_PASSWORD: pipeshubtest123
      POSTGRES_TEST_DB: pipeshub_connector_test
      # MariaDB, the same way: a source database in the compose stack.
      MARIADB_TEST_HOST: localhost
      MARIADB_TEST_PORT: '3307'
      MARIADB_CONNECTOR_HOST: mariadb-source
      MARIADB_CONNECTOR_PORT: '3306'
      MARIADB_TEST_USER: pipeshubtest
      MARIADB_TEST_PASSWORD: pipeshubtest123
      MARIADB_TEST_DB: pipeshub_connector_test
      # MinIO runs in the integration compose stack, so these are fixed
      # test values rather than secrets. The connector runs inside the
      # compose network and reaches MinIO by service name; the test process
      # reaches it on the published port.
      MINIO_TEST_ENDPOINT: http://localhost:9000
      MINIO_CONNECTOR_ENDPOINT: http://minio:9000
      MINIO_ROOT_USER: pipeshubtest
      MINIO_ROOT_PASSWORD: pipeshubtest123
      MINIO_TEST_BUCKET: pipeshub-connector-test
      # The Web and RSS connectors sync from the web-fixtures service in the
      # integration stack: the connector by service name, the test process
      # (which changes pages through its control API) on the published port.
      WEB_FIXTURES_TEST_URL: http://localhost:8089
      WEB_FIXTURES_CONNECTOR_URL: http://web-fixtures:8080
      # Nextcloud and BookStack run in the compose stack too, behind the
      # selfhosted-sources profile: only the shard that runs their tests
      # (connectors-1, or a dispatch of just one of them) starts them.
      # The Neo4j stack keeps Kafka behind the kafka profile, so a Kafka
      # run has to enable it or the app has no broker to reach. Compose
      # ignores the empty entry left when only one profile is on.
      NEXTCLOUD_TEST_URL: http://localhost:8090
      NEXTCLOUD_CONNECTOR_URL: http://nextcloud-source
      BOOKSTACK_TEST_URL: http://localhost:8094
      BOOKSTACK_CONNECTOR_URL: http://bookstack-source
      BOOKSTACK_DB_PORT: '3308'
      ZAMMAD_TEST_URL: http://localhost:8096
      ZAMMAD_CONNECTOR_URL: http://zammad-railsserver:3000
      COMPOSE_PROFILES: ${{ format('{0},{1},{2}', (matrix.shard == 'connectors-1' || (matrix.shard == 'dispatch' && (github.event.inputs.connectors == 'nextcloud' || github.event.inputs.connectors == 'bookstack'))) && 'selfhosted-sources' || '', (matrix.shard == 'connectors-3' || (matrix.shard == 'dispatch' && github.event.inputs.connectors == 'zammad')) && 'zammad-source' || '', needs.setup.outputs.message_broker == 'kafka' && 'kafka' || '') }}
      S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
      S3_REGION: ${{ secrets.S3_REGION }}
      S3_BUCKET: ${{ secrets.S3_BUCKET }}
      GCS_SERVICE_ACCOUNT_JSON: ${{ secrets.GCS_SERVICE_ACCOUNT_JSON }}
      AZURE_BLOB_CONNECTION_STRING: ${{ secrets.AZURE_BLOB_CONNECTION_STRING }}
      AZURE_FILES_CONNECTION_STRING: ${{ secrets.AZURE_FILES_CONNECTION_STRING }}
      TEST_OPENAI_LLM_MODEL: ${{ vars.TEST_OPENAI_LLM_MODEL }}
      # Azure OpenAI is where the credits are, and TEST_AI_MODEL_PROVIDER names
      # it outright: the helper falls back to the next provider on a health
      # check failure, so without this an Azure problem retries against the
      # OpenAI key and reports that instead.
      TEST_AI_MODEL_PROVIDER: azureOpenAI
      # _LLM_PROVIDER_ORDER tries Azure before OpenAI, but _azure_credentials_complete
      # needs key, endpoint and deployment together — miss one and Azure is dropped
      # from the candidates silently, leaving OpenAI as the only provider tried.
      # secrets || vars so the non-secret names work from either store.
      TEST_AZURE_OPENAI_API_KEY: ${{ secrets.TEST_AZURE_OPENAI_API_KEY }}
      TEST_AZURE_OPENAI_ENDPOINT: ${{ secrets.TEST_AZURE_OPENAI_ENDPOINT || vars.TEST_AZURE_OPENAI_ENDPOINT }}
      TEST_AZURE_OPENAI_DEPLOYMENT_NAME: ${{ secrets.TEST_AZURE_OPENAI_DEPLOYMENT_NAME || vars.TEST_AZURE_OPENAI_DEPLOYMENT_NAME }}
      TEST_AZURE_OPENAI_MODEL: ${{ secrets.TEST_AZURE_OPENAI_MODEL || vars.TEST_AZURE_OPENAI_MODEL }}
      # Optional. Unset falls back to the LLM deployment above, which is what a
      # local .env run does today.
      TEST_AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME: ${{ secrets.TEST_AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME || vars.TEST_AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME }}
      TEST_AZURE_OPENAI_EMBEDDING_MODEL: ${{ secrets.TEST_AZURE_OPENAI_EMBEDDING_MODEL || vars.TEST_AZURE_OPENAI_EMBEDDING_MODEL }}
      CONFLUENCE_TEST_BASE_URL: ${{ secrets.CONFLUENCE_TEST_BASE_URL }}
      CONFLUENCE_TEST_EMAIL: ${{ secrets.CONFLUENCE_TEST_EMAIL }}
      CONFLUENCE_TEST_API_TOKEN: ${{ secrets.CONFLUENCE_TEST_API_TOKEN }}
      CONFLUENCE_TEST_SPACE_KEY: ${{ secrets.CONFLUENCE_TEST_SPACE_KEY }}
      SHAREPOINT_TEST_SITE_NAMES: ${{ secrets.SHAREPOINT_TEST_SITE_NAMES }}
      SHAREPOINT_TEST_EXPECTED_USER_EMAILS: ${{ secrets.SHAREPOINT_TEST_EXPECTED_USER_EMAILS }}
      SHAREPOINT_TEST_TENANT_ID: ${{ secrets.SHAREPOINT_TEST_TENANT_ID }}
      SHAREPOINT_TEST_CLIENT_ID: ${{ secrets.SHAREPOINT_TEST_CLIENT_ID }}
      SHAREPOINT_TEST_SHAREPOINT_DOMAIN: ${{ secrets.SHAREPOINT_TEST_SHAREPOINT_DOMAIN }}
      # No _CONTENT suffix: the secrets are stored under these names, and a
      # reference to a secret that does not exist resolves to empty, which the
      # suite reports as "the SharePoint tenant is not configured" -- the same
      # message as having no credentials at all.
      SHAREPOINT_TEST_CERTIFICATE_FILE: ${{ secrets.SHAREPOINT_TEST_CERTIFICATE_FILE }}
      SHAREPOINT_TEST_PRIVATE_KEY_FILE: ${{ secrets.SHAREPOINT_TEST_PRIVATE_KEY_FILE }}
      JIRA_TEST_BASE_URL: ${{ secrets.JIRA_TEST_BASE_URL }}
      JIRA_TEST_EMAIL: ${{ secrets.JIRA_TEST_EMAIL }}
      JIRA_TEST_API_TOKEN: ${{ secrets.JIRA_TEST_API_TOKEN }}
      JIRA_TEST_PROJECT_KEYS: ${{ secrets.JIRA_TEST_PROJECT_KEYS }}
      LINEAR_TEST_API_TOKEN: ${{ secrets.LINEAR_TEST_API_TOKEN }}
      LINEAR_TEST_TEAM_IDS: ${{ secrets.LINEAR_TEST_TEAM_IDS }}
      # GitHub Teams. Secret names may not start with GITHUB_ (reserved by Actions),
      # hence the GH_ prefix. The token is a classic PAT — not fine-grained, which
      # cannot reach GraphQL, used by the code-file timestamp backfill.
      # GitHub Teams. Secret names cannot start with GITHUB_ (reserved by Actions).
      GH_TEAMS_TEST_TOKEN: ${{ secrets.GH_TEAMS_TEST_TOKEN }}
      GH_TEAMS_TEST_ORG: ${{ secrets.GH_TEAMS_TEST_ORG }}
      GH_TEAMS_TEST_PRIMARY_REPO: ${{ secrets.GH_TEAMS_TEST_PRIMARY_REPO }}
      GH_TEAMS_TEST_PUBLIC_REPO: ${{ secrets.GH_TEAMS_TEST_PUBLIC_REPO }}
      GH_TEAMS_TEST_MUTATION_REPO: ${{ secrets.GH_TEAMS_TEST_MUTATION_REPO }}
      # GitLab. INSTANCE_URL is blank/https://gitlab.com for cloud, or the
      # self-managed host for EE.
      GITLAB_TEST_TOKEN: ${{ secrets.GITLAB_TEST_TOKEN }}
      GITLAB_TEST_INSTANCE_URL: ${{ secrets.GITLAB_TEST_INSTANCE_URL }}
      GITLAB_TEST_GROUP: ${{ secrets.GITLAB_TEST_GROUP }}
      GITLAB_TEST_SUBGROUP: ${{ secrets.GITLAB_TEST_SUBGROUP }}
      GITLAB_TEST_PRIMARY_PROJECT: ${{ secrets.GITLAB_TEST_PRIMARY_PROJECT }}
      GITLAB_TEST_MUTATION_PROJECT: ${{ secrets.GITLAB_TEST_MUTATION_PROJECT }}
      NOTION_TEST_API_TOKEN: ${{ secrets.NOTION_TEST_API_TOKEN }}
      NOTION_TEST_ROOT_PAGE_ID: ${{ secrets.NOTION_TEST_ROOT_PAGE_ID }}
      NOTION_TEST_ROOT_DATABASE_ID: ${{ secrets.NOTION_TEST_ROOT_DATABASE_ID }}
      # Google Drive Workspace (service account + domain-wide delegation)
      GOOGLE_DRIVE_WORKSPACE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_DRIVE_WORKSPACE_SERVICE_ACCOUNT_JSON }}
      GOOGLE_DRIVE_WORKSPACE_ADMIN_EMAIL: ${{ secrets.GOOGLE_DRIVE_WORKSPACE_ADMIN_EMAIL }}
      GOOGLE_DRIVE_WORKSPACE_TEST_USER_EMAIL: ${{ secrets.GOOGLE_DRIVE_WORKSPACE_TEST_USER_EMAIL }}
      GOOGLE_DRIVE_WORKSPACE_SECOND_USER_EMAIL: ${{ secrets.GOOGLE_DRIVE_WORKSPACE_SECOND_USER_EMAIL }}
      # Google Drive personal (OAuth). Refresh token is normally in
      # .env.refresh_tokens locally; CI injects it as an env secret.
      GOOGLE_DRIVE_CLIENT_ID: ${{ secrets.GOOGLE_DRIVE_CLIENT_ID }}
      GOOGLE_DRIVE_CLIENT_SECRET: ${{ secrets.GOOGLE_DRIVE_CLIENT_SECRET }}
      GOOGLE_DRIVE_TEST_USER_EMAIL: ${{ secrets.GOOGLE_DRIVE_TEST_USER_EMAIL }}
      GOOGLE_DRIVE_REFRESH_TOKEN: ${{ secrets.GOOGLE_DRIVE_REFRESH_TOKEN }}

      SMTP_HOST: mailpit
      SMTP_PORT: '1025'
      SMTP_FROM_EMAIL: no-reply@example.com
      # Connector shards, one source of truth. The `core` shard is everything
      # these three do not own, built in the test step from the same values.
      # Balanced on measured minutes across both graph legs (both legs run in
      # one job, so a shard's wall clock is the sum of the two):
      # shard 1 ~136 min, shard 2 ~135 min, shard 3 ~132 min. Shard 1 keeps
      # nextcloud and bookstack, the only suites needing selfhosted-sources.
      # Rebalance with scripts/shard_balance.py when a suite grows.
      CONN_SHARD_1: "nextcloud or bookstack or mariadb or minio or postgres or web or rss or local_fs or github or sharepoint"
      CONN_SHARD_2: "google_drive_workspace or google_drive_individual or gitlab or jira or linear or azure_blob or azure_files"
      CONN_SHARD_3: "notion or github_teams or confluence or s3 or gcs or zammad"
      # Routed through env (never interpolated into the shell) so untrusted
      # dispatch input cannot inject; the test steps pick markers from these.
      CONNECTORS_INPUT: ${{ github.event.inputs.connectors }}
      SHARD: ${{ matrix.shard }}
      # Playwright runs once: on the neo4j/both leg, or the arango leg only when
      # arango is the sole backend (arango dispatch input, or it-arango alone).
      RUN_PLAYWRIGHT: ${{ (matrix.shard == 'core' || matrix.shard == 'dispatch') && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both' || (matrix.graph_db == 'arango' && (github.event.inputs.graph_db == 'arango' || (contains(github.event.pull_request.labels.*.name, 'it-arango') && !contains(github.event.pull_request.labels.*.name, 'it-neo4j') && !contains(github.event.pull_request.labels.*.name, 'it-parallel'))))) && 'true' || 'false' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.event.inputs.commit_hash || github.event.pull_request.head.sha || github.sha }}
          allow-unsafe-pr-checkout: true
          persist-credentials: false

      - name: Set up Node.js
        if: env.RUN_PLAYWRIGHT == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install frontend deps and Playwright browsers
        if: env.RUN_PLAYWRIGHT == 'true'
        working-directory: frontend
        run: |
          npm ci
          npx playwright install chromium --with-deps

      # App Dockerfile uses these as FROM stages; pulling first warms the local
      # cache so compose build does not rebuild Dockerfile.base on every run.
      - name: Pull pre-built base images
        run: |
          docker pull pipeshubai/pipeshub-ai-base:python-deps || true
          docker pull pipeshubai/pipeshub-ai-base:runtime || true

      - name: Build Neo4j stack images
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.neo4j.yml build

      - name: Build ArangoDB stack images
        # Run even if the Neo4j pass failed, but stop if the run was cancelled.
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.arango.yml build

      - name: Set up Python ${{ github.event.inputs.python_version || '3.12' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ github.event.inputs.python_version || '3.12' }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python and install dependencies
        working-directory: integration-tests
        run: |
          uv venv
          uv pip install -e .

      # Pure helper unit tests (mocks only) — fail fast before Docker/services come up.
      - name: Run helper unit tests
        working-directory: integration-tests
        run: |
          source .venv/bin/activate
          mkdir -p reports
          pytest unit/ -v --tb=short --junitxml=reports/unit-results.xml

      - name: 'Neo4j: Start services'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.neo4j.yml up -d

      - name: 'Neo4j: Wait for core services'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        run: |
          echo "Waiting for core services..."
          timeout 300 bash -c '
            delay=1
            until curl -sf ${{ env.PIPESHUB_BASE_URL }}/api/v1/health | grep -q "\"status\":\"healthy\""; do
              echo "Not ready, retrying in ${delay}s..."
              sleep $delay
              delay=$((delay * 2))
              if [ $delay -gt 64 ]; then delay=64; fi
            done
          '
          echo "Core services are healthy!"

      - name: 'Neo4j: Wait for Python microservices'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        run: |
          echo "Waiting for connector and query services..."
          timeout 300 bash -c '
            delay=1
            until curl -sf ${{ env.PIPESHUB_BASE_URL }}/api/v1/health/services | grep -q "\"status\":\"healthy\""; do
              echo "Not ready, retrying in ${delay}s..."
              sleep $delay
              delay=$((delay * 2))
              if [ $delay -gt 64 ]; then delay=64; fi
            done
          '
          echo "All services are healthy!"

      - name: 'Neo4j: Create organization and admin user'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        run: |
          echo "Creating organization..."
          # jq reads the credentials from the environment, so quotes or backslashes
          # in a secret stay valid JSON and never appear on a command line.
          jq -n --arg name "Integration Test" --arg short "INTTEST" '{
              accountType: "business",
              adminFullName: "\($name) Admin",
              contactEmail: $ENV.PIPESHUB_TEST_USER_EMAIL,
              password: $ENV.PIPESHUB_TEST_USER_PASSWORD,
              confirmPassword: $ENV.PIPESHUB_TEST_USER_PASSWORD,
              registeredName: "\($name) Org",
              shortName: $short
            }' | curl -sf "${PIPESHUB_BASE_URL}/api/v1/org" \
            -H "Content-Type: application/json" \
            --data-binary @-
          echo ""
          echo "Organization created successfully!"

      - name: 'Neo4j: Skip onboarding'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        run: |
          HEADERS=$(mktemp)
          jq -n '{email: $ENV.PIPESHUB_TEST_USER_EMAIL}' \
            | curl -sf "${PIPESHUB_BASE_URL}/api/v1/userAccount/initAuth" \
            -H "Content-Type: application/json" \
            --data-binary @- \
            -D "${HEADERS}" -o /dev/null
          SESSION_TOKEN=$(grep -i "x-session-token:" "${HEADERS}" | tr -d '\r' | awk '{print $2}')
          rm -f "${HEADERS}"

          ACCESS_TOKEN=$(jq -n '{
              method: "password",
              credentials: {password: $ENV.PIPESHUB_TEST_USER_PASSWORD},
              email: $ENV.PIPESHUB_TEST_USER_EMAIL
            }' | curl -sf "${PIPESHUB_BASE_URL}/api/v1/userAccount/authenticate" \
            -H "Content-Type: application/json" \
            -H "x-session-token: ${SESSION_TOKEN}" \
            --data-binary @- \
            | jq -r '.accessToken')

          curl -sf -X PUT "${PIPESHUB_BASE_URL}/api/v1/org/onboarding-status" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${ACCESS_TOKEN}" \
            -d '{"status": "configured"}'
          echo ""
          echo "Onboarding marked as configured!"

      - name: 'Neo4j: Run integration tests'
        if: matrix.graph_db == 'neo4j' || matrix.graph_db == 'both'
        working-directory: integration-tests
        env:
          TEST_GRAPH_DB_TYPE: neo4j
        run: |
          source .venv/bin/activate
          # Each shard runs its slice; `core` is everything the three connector
          # lists do not own, built here so the lists live in one place. The
          # single "dispatch" job (a one-marker workflow_dispatch) runs exactly
          # the selected marker.
          case "$SHARD" in
            dispatch)     MARKERS="$CONNECTORS_INPUT" ;;
            connectors-1) MARKERS="$CONN_SHARD_1" ;;
            connectors-2) MARKERS="$CONN_SHARD_2" ;;
            connectors-3) MARKERS="$CONN_SHARD_3" ;;
            core)         MARKERS="integration and not ($CONN_SHARD_1) and not ($CONN_SHARD_2) and not ($CONN_SHARD_3) and not storage" ;;
            *)            echo "Unknown shard '$SHARD'"; exit 1 ;;
          esac
          # -n 1 still forks a worker, so shared_session_resource treats the run as
          # distributed and skips teardown. -n 0 runs in-process and restores it.
          WORKERS="$PYTEST_WORKERS"
          if [ "$WORKERS" = "1" ]; then WORKERS=0; fi
          # Resilience tests restart shared services, so never two at once.
          if [ "$MARKERS" = "resilience" ]; then WORKERS=0; fi
          # Each xdist worker is its own session, and the storage suite repoints
          # the whole deployment per session — two at once would fight.
          if [ "$MARKERS" = "storage" ]; then WORKERS=0; fi
          # No exit-code special-casing: an empty selection (pytest exit 5) fails.
          # On a normal run that means a shard's marker was dropped or renamed; on
          # a dispatch it means the chosen marker matches no tests (e.g. messaging).
          pytest -m "$MARKERS" -v --tb=short \
            -n "$WORKERS" --dist loadgroup \
            --junitxml=reports/neo4j-results.xml 2>&1



      - name: 'Neo4j: Run Playwright e2e tests'
        if: ${{ !cancelled() && env.RUN_PLAYWRIGHT == 'true' && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both') }}
        working-directory: frontend
        env:
          CI: 'true'
          PLAYWRIGHT_NO_SERVER: 'true'
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_API_BASE_URL: http://localhost:3000
          TEST_USER_EMAIL: ${{ env.PIPESHUB_TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ env.PIPESHUB_TEST_USER_PASSWORD }}
        run: npm run test:e2e 2>&1

      # Restarts Redis, Qdrant, the graph database and MongoDB, kills the
      # indexing process, cuts the app off from its AI provider and makes blob
      # storage unwritable on the live stack, which would break any test
      # running beside it. So it runs alone,
      # after everything else on this stack, and only nightly on the core
      # shard's Neo4j pass: one stack, no extra runner, no time added to a PR.
      # A dispatch with connectors=resilience runs it through the step above.
      - name: 'Neo4j: Run resilience tests'
        if: ${{ !cancelled() && github.event_name == 'schedule' && matrix.shard == 'core' && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both') }}
        working-directory: integration-tests
        env:
          TEST_GRAPH_DB_TYPE: neo4j
        run: |
          source .venv/bin/activate
          pytest -m resilience -v --tb=short \
            --junitxml=reports/neo4j-resilience-results.xml 2>&1

      # These repoint the whole deployment's storage while they run, so they go
      # last, after everything else has finished with this stack.
      - name: 'Neo4j: Run storage-backend tests'
        if: ${{ !cancelled() && github.event_name == 'schedule' && matrix.shard == 'core' && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both') }}
        working-directory: integration-tests
        env:
          TEST_GRAPH_DB_TYPE: neo4j
        run: |
          source .venv/bin/activate
          pytest -m storage -v --tb=short \
            --junitxml=reports/neo4j-storage-results.xml 2>&1

      - name: 'Neo4j: Dump docker logs'
        if: always() && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both')
        working-directory: deployment/docker-compose
        run: |
          mkdir -p ../../integration-tests/reports/docker-logs/neo4j
          docker compose -f docker-compose.integration.neo4j.yml ps -a \
            > ../../integration-tests/reports/docker-logs/neo4j/_containers.txt 2>&1 || true
          for svc in $(docker compose -f docker-compose.integration.neo4j.yml config --services); do
            docker compose -f docker-compose.integration.neo4j.yml logs --no-color --timestamps "$svc" \
              > "../../integration-tests/reports/docker-logs/neo4j/${svc}.log" 2>&1 || true
          done

      - name: 'Neo4j: Tear down services (preserve images)'
        if: always() && (matrix.graph_db == 'neo4j' || matrix.graph_db == 'both')
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.neo4j.yml down -v

      - name: 'ArangoDB: Start services'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.arango.yml up -d

      - name: 'ArangoDB: Wait for core services'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        run: |
          echo "Waiting for core services..."
          timeout 300 bash -c '
            delay=1
            until curl -sf ${{ env.PIPESHUB_BASE_URL }}/api/v1/health | grep -q "\"status\":\"healthy\""; do
              echo "Not ready, retrying in ${delay}s..."
              sleep $delay
              delay=$((delay * 2))
              if [ $delay -gt 64 ]; then delay=64; fi
            done
          '
          echo "Core services are healthy!"

      - name: 'ArangoDB: Wait for Python microservices'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        run: |
          echo "Waiting for connector and query services..."
          timeout 300 bash -c '
            delay=1
            until curl -sf ${{ env.PIPESHUB_BASE_URL }}/api/v1/health/services | grep -q "\"status\":\"healthy\""; do
              echo "Not ready, retrying in ${delay}s..."
              sleep $delay
              delay=$((delay * 2))
              if [ $delay -gt 64 ]; then delay=64; fi
            done
          '
          echo "All services are healthy!"

      - name: 'ArangoDB: Create organization and admin user'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        run: |
          echo "Creating organization..."
          # jq reads the credentials from the environment, so quotes or backslashes
          # in a secret stay valid JSON and never appear on a command line.
          jq -n --arg name "Integration Test" --arg short "INTTEST" '{
              accountType: "business",
              adminFullName: "\($name) Admin",
              contactEmail: $ENV.PIPESHUB_TEST_USER_EMAIL,
              password: $ENV.PIPESHUB_TEST_USER_PASSWORD,
              confirmPassword: $ENV.PIPESHUB_TEST_USER_PASSWORD,
              registeredName: "\($name) Org",
              shortName: $short
            }' | curl -sf "${PIPESHUB_BASE_URL}/api/v1/org" \
            -H "Content-Type: application/json" \
            --data-binary @-
          echo ""
          echo "Organization created successfully!"

      - name: 'ArangoDB: Skip onboarding'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        run: |
          HEADERS=$(mktemp)
          jq -n '{email: $ENV.PIPESHUB_TEST_USER_EMAIL}' \
            | curl -sf "${PIPESHUB_BASE_URL}/api/v1/userAccount/initAuth" \
            -H "Content-Type: application/json" \
            --data-binary @- \
            -D "${HEADERS}" -o /dev/null
          SESSION_TOKEN=$(grep -i "x-session-token:" "${HEADERS}" | tr -d '\r' | awk '{print $2}')
          rm -f "${HEADERS}"

          ACCESS_TOKEN=$(jq -n '{
              method: "password",
              credentials: {password: $ENV.PIPESHUB_TEST_USER_PASSWORD},
              email: $ENV.PIPESHUB_TEST_USER_EMAIL
            }' | curl -sf "${PIPESHUB_BASE_URL}/api/v1/userAccount/authenticate" \
            -H "Content-Type: application/json" \
            -H "x-session-token: ${SESSION_TOKEN}" \
            --data-binary @- \
            | jq -r '.accessToken')

          curl -sf -X PUT "${PIPESHUB_BASE_URL}/api/v1/org/onboarding-status" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${ACCESS_TOKEN}" \
            -d '{"status": "configured"}'
          echo ""
          echo "Onboarding marked as configured!"

      - name: 'ArangoDB: Run integration tests'
        if: ${{ !cancelled() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both') }}
        working-directory: integration-tests
        env:
          TEST_GRAPH_DB_TYPE: arango
          TEST_ARANGO_URL: http://localhost:8529
          TEST_ARANGO_USERNAME: root
          TEST_ARANGO_PASSWORD: ${{ env.ARANGO_PASSWORD }}
          TEST_ARANGO_DB_NAME: es
        run: |
          source .venv/bin/activate
          # Each shard runs its slice; `core` is everything the three connector
          # lists do not own, built here so the lists live in one place. The
          # single "dispatch" job (a one-marker workflow_dispatch) runs exactly
          # the selected marker.
          case "$SHARD" in
            dispatch)     MARKERS="$CONNECTORS_INPUT" ;;
            connectors-1) MARKERS="$CONN_SHARD_1" ;;
            connectors-2) MARKERS="$CONN_SHARD_2" ;;
            connectors-3) MARKERS="$CONN_SHARD_3" ;;
            core)         MARKERS="integration and not ($CONN_SHARD_1) and not ($CONN_SHARD_2) and not ($CONN_SHARD_3) and not storage" ;;
            *)            echo "Unknown shard '$SHARD'"; exit 1 ;;
          esac
          # -n 1 still forks a worker, so shared_session_resource treats the run as
          # distributed and skips teardown. -n 0 runs in-process and restores it.
          WORKERS="$PYTEST_WORKERS"
          if [ "$WORKERS" = "1" ]; then WORKERS=0; fi
          # Resilience tests restart shared services, so never two at once.
          if [ "$MARKERS" = "resilience" ]; then WORKERS=0; fi
          # Each xdist worker is its own session, and the storage suite repoints
          # the whole deployment per session — two at once would fight.
          if [ "$MARKERS" = "storage" ]; then WORKERS=0; fi
          # No exit-code special-casing: an empty selection (pytest exit 5) fails.
          # On a normal run that means a shard's marker was dropped or renamed; on
          # a dispatch it means the chosen marker matches no tests (e.g. messaging).
          pytest -m "$MARKERS" -v --tb=short \
            -n "$WORKERS" --dist loadgroup \
            --junitxml=reports/arango-results.xml 2>&1

      - name: 'ArangoDB: Run Playwright e2e tests'
        if: ${{ !cancelled() && matrix.graph_db == 'arango' && env.RUN_PLAYWRIGHT == 'true' }}
        working-directory: frontend
        env:
          CI: 'true'
          PLAYWRIGHT_NO_SERVER: 'true'
          BASE_URL: http://localhost:3000
          NEXT_PUBLIC_API_BASE_URL: http://localhost:3000
          TEST_USER_EMAIL: ${{ env.PIPESHUB_TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ env.PIPESHUB_TEST_USER_PASSWORD }}
        run: npm run test:e2e 2>&1

      - name: 'ArangoDB: Dump docker logs'
        if: always() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both')
        working-directory: deployment/docker-compose
        run: |
          mkdir -p ../../integration-tests/reports/docker-logs/arango
          docker compose -f docker-compose.integration.arango.yml ps -a \
            > ../../integration-tests/reports/docker-logs/arango/_containers.txt 2>&1 || true
          for svc in $(docker compose -f docker-compose.integration.arango.yml config --services); do
            docker compose -f docker-compose.integration.arango.yml logs --no-color --timestamps "$svc" \
              > "../../integration-tests/reports/docker-logs/arango/${svc}.log" 2>&1 || true
          done

      - name: 'ArangoDB: Tear down services (preserve images)'
        if: always() && (matrix.graph_db == 'arango' || matrix.graph_db == 'both')
        working-directory: deployment/docker-compose
        run: docker compose -f docker-compose.integration.arango.yml down -v

      - name: Collect Playwright artifacts into reports
        if: always() && env.RUN_PLAYWRIGHT == 'true'
        run: |
          mkdir -p integration-tests/reports/playwright
          cp -r frontend/playwright-report integration-tests/reports/playwright/html-report 2>/dev/null || true
          cp frontend/test-results/playwright-junit.xml integration-tests/reports/playwright/ 2>/dev/null || true

      - name: Collect test results and prepare Slack report
        id: test-results
        if: ${{ !cancelled() }}
        working-directory: integration-tests
        env:
          # Route untrusted PR metadata through env vars so the shell script
          # below never evaluates `${{ ... }}` directly (workflow-injection).
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER_IN: ${{ github.event.pull_request.number }}
          PR_TITLE_IN: ${{ github.event.pull_request.title }}
          PR_URL_IN: ${{ github.event.pull_request.html_url }}
          PR_LOGIN_IN: ${{ github.event.pull_request.user.login }}
          ACTOR_IN: ${{ github.actor }}
          JOB_STATUS: ${{ job.status }}
          SETUP_LABEL: ${{ needs.setup.outputs.label }}
          SETUP_IS_DEFAULT: ${{ needs.setup.outputs.is_default }}
        run: |
          TOTAL_PASSED=0
          TOTAL_FAILED=0
          TOTAL_SKIPPED=0
          TOTAL_ERRORS=0
          TOTAL_TESTS=0
          BACKENDS_RUN=""

          for xml in reports/neo4j-results.xml reports/neo4j-resilience-results.xml reports/neo4j-storage-results.xml reports/arango-results.xml; do
            if [ -f "$xml" ]; then
              backend=$(basename "$xml" -results.xml)
              BACKENDS_RUN="${BACKENDS_RUN:+$BACKENDS_RUN, }$backend"
              tests=$(grep -oP 'tests="\K[0-9]+' "$xml" | head -1)
              failures=$(grep -oP 'failures="\K[0-9]+' "$xml" | head -1)
              errors=$(grep -oP 'errors="\K[0-9]+' "$xml" | head -1)
              skipped=$(grep -oP 'skipped="\K[0-9]+' "$xml" | head -1)
              TOTAL_TESTS=$((TOTAL_TESTS + ${tests:-0}))
              TOTAL_PASSED=$((TOTAL_PASSED + ${tests:-0} - ${failures:-0} - ${errors:-0} - ${skipped:-0}))
              TOTAL_FAILED=$((TOTAL_FAILED + ${failures:-0}))
              TOTAL_ERRORS=$((TOTAL_ERRORS + ${errors:-0}))
              TOTAL_SKIPPED=$((TOTAL_SKIPPED + ${skipped:-0}))
            fi
          done

          if [ "$TOTAL_TESTS" -gt 0 ]; then
            PASS_RATE=$(( (TOTAL_PASSED * 100) / TOTAL_TESTS ))
          else
            PASS_RATE=0
          fi

          if [ "$EVENT_NAME" = "pull_request_target" ]; then
            TRIGGER_LABEL="PR #${PR_NUMBER_IN}"
            AUTHOR="$PR_LOGIN_IN"
            # Slack mrkdwn escape for link labels (& < >). Build a clickable
            # `<url|#NUM — Title>` text for a dedicated *PR:* field.
            TITLE_SLACK=$(printf '%s' "$PR_TITLE_IN" | sed -e 's/&/\&amp;/g' -e 's/</\&lt;/g' -e 's/>/\&gt;/g')
            PR_FIELD_TEXT="*PR:*"$'\n'"<${PR_URL_IN}|#${PR_NUMBER_IN} — ${TITLE_SLACK}>"
          elif [ "$EVENT_NAME" = "schedule" ]; then
            TRIGGER_LABEL="Scheduled (daily 12 AM IST)"
            AUTHOR="$ACTOR_IN"
            PR_FIELD_TEXT=""
          else
            TRIGGER_LABEL="Manual Dispatch"
            AUTHOR="$ACTOR_IN"
            PR_FIELD_TEXT=""
          fi
          # Name a non-default setup in the headline, so a red run isn't read as
          # a regression on the setup most customers use.
          if [ "$SETUP_IS_DEFAULT" = "false" ]; then
            TRIGGER_LABEL="${TRIGGER_LABEL} · on ${SETUP_LABEL}"
          fi

          # Parse helper unit-test JUnit XML (runs before Docker; no graph backend).
          UNIT_TESTS=0
          UNIT_PASSED=0
          UNIT_FAILED=0
          UNIT_ERRORS=0
          UNIT_SKIPPED=0
          if [ -f "reports/unit-results.xml" ]; then
            unit_tests=$(grep -oP 'tests="\K[0-9]+' "reports/unit-results.xml" | head -1)
            unit_failures=$(grep -oP 'failures="\K[0-9]+' "reports/unit-results.xml" | head -1)
            unit_errors=$(grep -oP 'errors="\K[0-9]+' "reports/unit-results.xml" | head -1)
            unit_skipped=$(grep -oP 'skipped="\K[0-9]+' "reports/unit-results.xml" | head -1)
            UNIT_TESTS=${unit_tests:-0}
            UNIT_FAILED=${unit_failures:-0}
            UNIT_ERRORS=${unit_errors:-0}
            UNIT_SKIPPED=${unit_skipped:-0}
            UNIT_PASSED=$((UNIT_TESTS - UNIT_FAILED - UNIT_ERRORS - UNIT_SKIPPED))
          fi
          if [ "$UNIT_TESTS" -gt 0 ]; then
            UNIT_PASS_RATE=$(( (UNIT_PASSED * 100) / UNIT_TESTS ))
          else
            UNIT_PASS_RATE=0
          fi

          # Parse Playwright JUnit XML
          PW_TESTS=0
          PW_PASSED=0
          PW_FAILED=0
          PW_ERRORS=0
          PW_SKIPPED=0
          if [ -f "reports/playwright/playwright-junit.xml" ]; then
            pw_tests=$(grep -oP 'tests="\K[0-9]+' "reports/playwright/playwright-junit.xml" | head -1)
            pw_failures=$(grep -oP 'failures="\K[0-9]+' "reports/playwright/playwright-junit.xml" | head -1)
            pw_errors=$(grep -oP 'errors="\K[0-9]+' "reports/playwright/playwright-junit.xml" | head -1)
            pw_skipped=$(grep -oP 'skipped="\K[0-9]+' "reports/playwright/playwright-junit.xml" | head -1)
            PW_TESTS=${pw_tests:-0}
            PW_FAILED=${pw_failures:-0}
            PW_ERRORS=${pw_errors:-0}
            PW_SKIPPED=${pw_skipped:-0}
            PW_PASSED=$((PW_TESTS - PW_FAILED - PW_ERRORS - PW_SKIPPED))
          fi
          if [ "$PW_TESTS" -gt 0 ]; then
            PW_PASS_RATE=$(( (PW_PASSED * 100) / PW_TESTS ))
          else
            PW_PASS_RATE=0
          fi

          # Green only when suites pass AND the job is healthy — a stack that never
          # came up yields zero counts, which would otherwise read as success.
          OVERALL_OK=true
          if [ "$TOTAL_FAILED" -gt 0 ] || [ "$TOTAL_ERRORS" -gt 0 ] || [ "$UNIT_FAILED" -gt 0 ] || [ "$UNIT_ERRORS" -gt 0 ] || [ "$PW_FAILED" -gt 0 ] || [ "$PW_ERRORS" -gt 0 ]; then
            OVERALL_OK=false
          fi
          if [ "$JOB_STATUS" != "success" ] || [ "$TOTAL_TESTS" -eq 0 ]; then
            OVERALL_OK=false
          fi

          echo "TOTAL_TESTS=$TOTAL_TESTS" >> "$GITHUB_OUTPUT"
          echo "TOTAL_PASSED=$TOTAL_PASSED" >> "$GITHUB_OUTPUT"
          echo "TOTAL_FAILED=$TOTAL_FAILED" >> "$GITHUB_OUTPUT"
          echo "TOTAL_ERRORS=$TOTAL_ERRORS" >> "$GITHUB_OUTPUT"
          echo "TOTAL_SKIPPED=$TOTAL_SKIPPED" >> "$GITHUB_OUTPUT"
          echo "PASS_RATE=$PASS_RATE" >> "$GITHUB_OUTPUT"
          echo "UNIT_TESTS=$UNIT_TESTS" >> "$GITHUB_OUTPUT"
          echo "UNIT_PASSED=$UNIT_PASSED" >> "$GITHUB_OUTPUT"
          echo "UNIT_FAILED=$UNIT_FAILED" >> "$GITHUB_OUTPUT"
          echo "UNIT_ERRORS=$UNIT_ERRORS" >> "$GITHUB_OUTPUT"
          echo "UNIT_SKIPPED=$UNIT_SKIPPED" >> "$GITHUB_OUTPUT"
          echo "UNIT_PASS_RATE=$UNIT_PASS_RATE" >> "$GITHUB_OUTPUT"
          echo "PW_TESTS=$PW_TESTS" >> "$GITHUB_OUTPUT"
          echo "PW_PASSED=$PW_PASSED" >> "$GITHUB_OUTPUT"
          echo "PW_FAILED=$PW_FAILED" >> "$GITHUB_OUTPUT"
          echo "PW_ERRORS=$PW_ERRORS" >> "$GITHUB_OUTPUT"
          echo "PW_SKIPPED=$PW_SKIPPED" >> "$GITHUB_OUTPUT"
          echo "PW_PASS_RATE=$PW_PASS_RATE" >> "$GITHUB_OUTPUT"
          echo "OVERALL_OK=$OVERALL_OK" >> "$GITHUB_OUTPUT"
          echo "TRIGGER_LABEL=$TRIGGER_LABEL" >> "$GITHUB_OUTPUT"
          echo "AUTHOR=$AUTHOR" >> "$GITHUB_OUTPUT"
          echo "BACKENDS_RUN=$BACKENDS_RUN" >> "$GITHUB_OUTPUT"
          echo "IS_PR=$([ "$EVENT_NAME" = "pull_request_target" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
          # Use a heredoc so a title containing quotes or other special chars
          # can't break the output file format.
          {
            echo "PR_FIELD_TEXT<<PR_FIELD_EOF"
            printf '%s\n' "$PR_FIELD_TEXT"
            echo "PR_FIELD_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Zip reports folder
        if: ${{ !cancelled() }}
        working-directory: integration-tests
        run: |
          mkdir -p reports
          zip -r reports.zip reports/

      - name: Send Slack report
        if: ${{ !cancelled() }}
        id: slack-message
        uses: slackapi/slack-github-action@v2.1.0
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_BOT_TOKEN }}
          payload: |
            {
              "channel": "${{ vars.SLACK_CI_CHANNEL_ID }}",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "${{ steps.test-results.outputs.OVERALL_OK == 'true' && ':large_green_circle:' || ':red_circle:' }} Integration Tests (${{ matrix.graph_db }} · ${{ matrix.shard }}) — ${{ steps.test-results.outputs.TRIGGER_LABEL }}"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    ${{ steps.test-results.outputs.IS_PR == 'true' && format('{{ "type": "mrkdwn", "text": {0} }},', toJSON(steps.test-results.outputs.PR_FIELD_TEXT)) || '' }}
                    { "type": "mrkdwn", "text": "*Author:*\n<https://github.com/${{ steps.test-results.outputs.AUTHOR }}|@${{ steps.test-results.outputs.AUTHOR }}>" },
                    { "type": "mrkdwn", "text": "*Branch:*\n`${{ github.head_ref || github.ref_name }}`" },
                    { "type": "mrkdwn", "text": "*Graph DB:*\n${{ steps.test-results.outputs.BACKENDS_RUN || 'N/A' }}" },
                    { "type": "mrkdwn", "text": "*Connectors:*\n${{ github.event.inputs.connectors || 'integration (all)' }}" }
                  ]
                },
                {
                  "type": "section",
                  "text": { "type": "mrkdwn", "text": "*Test Results*" }
                },
                {
                  "type": "table",
                  "column_settings": [
                    { "align": "left"  },
                    { "align": "right" },
                    { "align": "right" },
                    { "align": "right" },
                    { "align": "right" },
                    { "align": "right" },
                    { "align": "right" }
                  ],
                  "rows": [
                    [
                      { "type": "raw_text", "text": "Suite"     },
                      { "type": "raw_text", "text": "Total"     },
                      { "type": "raw_text", "text": "Passed"    },
                      { "type": "raw_text", "text": "Failed"    },
                      { "type": "raw_text", "text": "Errors"    },
                      { "type": "raw_text", "text": "Skipped"   },
                      { "type": "raw_text", "text": "Pass Rate" }
                    ],
                    [
                      { "type": "raw_text", "text": "Helper unit (pytest)"                                           },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_TESTS }}"                 },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_PASSED }}"                },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_FAILED }}"                },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_ERRORS }}"                },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_SKIPPED }}"               },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.UNIT_PASS_RATE }}%"            }
                    ],
                    [
                      { "type": "raw_text", "text": "Backend (pytest)"                                              },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.TOTAL_TESTS }}"                },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.TOTAL_PASSED }}"               },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.TOTAL_FAILED }}"               },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.TOTAL_ERRORS }}"               },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.TOTAL_SKIPPED }}"              },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PASS_RATE }}%"                 }
                    ],
                    [
                      { "type": "raw_text", "text": "Frontend (Playwright)"                                         },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_TESTS }}"                   },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_PASSED }}"                  },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_FAILED }}"                  },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_ERRORS }}"                  },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_SKIPPED }}"                 },
                      { "type": "raw_text", "text": "${{ steps.test-results.outputs.PW_PASS_RATE }}%"              }
                    ]
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "${{ steps.test-results.outputs.OVERALL_OK == 'true' && ':tada: All tests passed!' || ':warning: Some tests failed or had errors. Check the workflow run for details.' }}"
                  }
                },
                {
                  "type": "context",
                  "elements": [
                    { "type": "mrkdwn", "text": "Commit: `${{ github.event.inputs.commit_hash || github.event.pull_request.head.sha || github.sha }}` • Job: `${{ job.status }}`" }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "View Workflow Run" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }

      - name: Upload reports zip to Slack
        if: ${{ !cancelled() }}
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          CHANNEL_ID: ${{ vars.SLACK_CI_CHANNEL_ID }}
          MESSAGE_RESPONSE: ${{ steps.slack-message.outputs.response || '{}' }}
        working-directory: integration-tests
        run: |
          if [ ! -f reports.zip ]; then
            echo "No reports.zip found, skipping upload"
            exit 0
          fi
          THREAD_TS=$(echo "$MESSAGE_RESPONSE" | jq -r '.ts // empty')
          FILE_SIZE=$(stat -c%s reports.zip)

          RESPONSE=$(curl -s -X POST "https://slack.com/api/files.getUploadURLExternal" \
            -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
            -d "filename=integration-test-reports-${{ matrix.graph_db }}-${{ matrix.shard }}.zip" \
            -d "length=$FILE_SIZE")
          OK=$(echo "$RESPONSE" | jq -r '.ok')
          if [ "$OK" != "true" ]; then
            echo "::warning::Failed to get Slack upload URL: $RESPONSE"
            exit 0
          fi
          UPLOAD_URL=$(echo "$RESPONSE" | jq -r '.upload_url')
          FILE_ID=$(echo "$RESPONSE" | jq -r '.file_id')

          curl -s -X POST "$UPLOAD_URL" -F "file=@reports.zip"

          COMPLETE_PAYLOAD="{\"files\":[{\"id\":\"$FILE_ID\",\"title\":\"Integration Test Reports (${{ matrix.graph_db }} · ${{ matrix.shard }})\"}],\"channel_id\":\"$CHANNEL_ID\""
          if [ -n "$THREAD_TS" ]; then
            COMPLETE_PAYLOAD="$COMPLETE_PAYLOAD,\"thread_ts\":\"$THREAD_TS\""
          fi
          COMPLETE_PAYLOAD="$COMPLETE_PAYLOAD}"
          curl -s -X POST "https://slack.com/api/files.completeUploadExternal" \
            -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
            -H "Content-Type: application/json" \
            -d "$COMPLETE_PAYLOAD"

      - name: Send failed tests list to Slack thread
        if: ${{ !cancelled() && (steps.test-results.outputs.TOTAL_FAILED != '0' || steps.test-results.outputs.TOTAL_ERRORS != '0' || steps.test-results.outputs.UNIT_FAILED != '0' || steps.test-results.outputs.UNIT_ERRORS != '0' || steps.test-results.outputs.PW_FAILED != '0' || steps.test-results.outputs.PW_ERRORS != '0') }}
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          CHANNEL_ID: ${{ vars.SLACK_CI_CHANNEL_ID }}
          MESSAGE_RESPONSE: ${{ steps.slack-message.outputs.response || '{}' }}
          GRAPH_DB: ${{ matrix.graph_db }}
        working-directory: integration-tests
        run: |
          python3 - << 'PYEOF'
          import sys, json, os, xml.etree.ElementTree as ET, urllib.request, urllib.error

          msg_response = os.environ.get('MESSAGE_RESPONSE', '{}')
          channel_id   = os.environ.get('CHANNEL_ID', '')
          bot_token    = os.environ.get('SLACK_BOT_TOKEN', '')
          graph_db     = os.environ.get('GRAPH_DB', '')

          try:
              thread_ts = json.loads(msg_response).get('ts', '')
          except Exception:
              thread_ts = ''

          if not thread_ts:
              print("No thread_ts available, skipping failed-tests message")
              sys.exit(0)

          names = []
          for xml_path in [
              'reports/unit-results.xml',
              'reports/neo4j-results.xml',
              'reports/neo4j-resilience-results.xml',
              'reports/neo4j-storage-results.xml',
              'reports/arango-results.xml',
              'reports/playwright/playwright-junit.xml',
          ]:
              if not os.path.exists(xml_path):
                  continue
              backend = os.path.basename(xml_path).replace('-results.xml', '')
              try:
                  for tc in ET.parse(xml_path).findall('.//testcase'):
                      if tc.find('failure') is not None or tc.find('error') is not None:
                          classname = tc.get('classname', '')
                          test_name = tc.get('name', '')
                          label     = f"{classname}::{test_name}" if classname else test_name
                          names.append(f"[{backend}] {label}")
              except Exception as e:
                  print(f"Warning: could not parse {xml_path}: {e}", file=sys.stderr)

          if not names:
              print("No failed test names found in XML reports")
              sys.exit(0)

          BODY_LIMIT  = 38_000
          body        = "\n".join(names)
          if len(body) > BODY_LIMIT:
              truncated = body[:BODY_LIMIT].rsplit("\n", 1)[0]
              omitted   = len(names) - truncated.count("\n") - 1
              body      = truncated + f"\n… ({omitted} more — see reports zip for full list)"
          text    = f"*Failed Tests ({graph_db}):*\n```\n" + body + "\n```"
          payload = json.dumps({"channel": channel_id, "thread_ts": thread_ts, "text": text}).encode()
          req     = urllib.request.Request(
              "https://slack.com/api/chat.postMessage",
              data=payload,
              headers={"Authorization": f"Bearer {bot_token}", "Content-Type": "application/json"},
              method="POST",
          )
          try:
              with urllib.request.urlopen(req) as resp:
                  print(resp.read().decode())
          except urllib.error.URLError as e:
              print(f"::warning::Slack postMessage failed: {e}", file=sys.stderr)
              sys.exit(1)
          PYEOF

      # A green run that skipped a connector entirely looks like a run that
      # tested it. Say which is which on the job page.
      - name: Summarise which connector suites ran
        if: ${{ !cancelled() }}
        working-directory: integration-tests
        run: |
          python report_connector_coverage.py \
            reports/neo4j-results.xml \
            reports/neo4j-resilience-results.xml \
            reports/arango-results.xml >> "$GITHUB_STEP_SUMMARY"

      - name: Upload reports artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports-${{ matrix.graph_db }}-${{ matrix.shard }}
          path: integration-tests/reports/
          if-no-files-found: ignore
